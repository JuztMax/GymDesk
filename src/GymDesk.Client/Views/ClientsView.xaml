<Window x:Class="GymDesk.Client.ClientsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление клиентами" Height="500" Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <!-- Верхняя панель с кнопками -->
            <RowDefinition Height="Auto"/>
            <!-- Таблица данных -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель кнопок -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="➕ Добавить" Click="AddClient_Click" 
                    Padding="10,5" Margin="0,0,10,0" FontSize="14"/>

            <Button Content="️ Редактировать" Click="EditClient_Click" 
                    Padding="10,5" Margin="0,0,10,0" FontSize="14"/>

            <Button Content="🗑️ Удалить" Click="DeleteClient_Click" 
                    Padding="10,5" Background="#FFE0E0" FontSize="14"/>
        </StackPanel>

        <!-- Таблица клиентов -->
        <DataGrid x:Name="ClientsDataGrid" 
                  Grid.Row="1"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  AlternatingRowBackground="#F8F8F8"
                  RowHeight="30"
                  FontSize="14"/>
    </Grid>
</Window>